#include <stdio.h>
#include <math.h>

void main()
{
    int num = 6;
    double num2 = 5.4;

    printf("%f", pow(4, 3) );
    printf("\n%f", sqrt(36) );
    printf("\n%f", ceil(36.234) );
    printf("\n%f", floor(36.234) );
}