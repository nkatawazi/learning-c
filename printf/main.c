#include <stdio.h>

void main()
{
    /*
    format specifiers to print out particular data types
    %d for integers
    %s for strings
    %c for character
    %f for double
    */
    printf("My favourite %c is %f", 'n', 500.234);
}