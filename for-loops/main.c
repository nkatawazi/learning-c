#include <stdio.h>
#include <stdlib.h>
#include <string.h>

void main()
{
    int luckyNumbers[] = {5, 10, 15, 20, 25};
    // int arraySize = sizeof(luckyNumbers);
    // printf("%d\n", arraySize);

    for(int i = 0; i <= 5; i++)
    {
        printf("%d\n", luckyNumbers[i]);
    }

    
}